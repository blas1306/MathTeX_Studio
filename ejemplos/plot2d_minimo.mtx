# Hito 1: plot 2D minimo compatible

y = [1, 4, 2, 5, 3]
\plot(y)
\hold(on)

x = [0, 1, 2, 3, 4]
y1 = [0, 1, 4, 9, 16]
y2 = [0, 1, 8, 27, 64]

\title("Curvas 2D")
\xlabel("x")
\ylabel("y")
\grid(on)

\plot(x, y1, "r--")
\hold(on)
\plot(x, y2, "bo")
