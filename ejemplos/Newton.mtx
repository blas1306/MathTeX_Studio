function [x, iter, res] = Nr(f,x0,tol)

    g(x) = \diff(f, x);
    x = x0;
    iter = 0;
    
    res = \abs(f(x));

    while res > tol
        x = x - f(x)/g(x);
        res = abs(f(x));
        iter = iter + 1;
    end
end