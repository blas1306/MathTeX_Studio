function [x, iter, res] = blas(f, x0, tol)
    x = x0;
    iter = 0;
    
    res = \abs(f(x));
    
    while res > tol
        r = 9*(a^2 - 3*b)*x + a^3 - 27*c;
        x = \frac{-a + \nthroot(r, 3)}{3};
        res = \abs(f(x));
        iter = iter + 1;
    end
end